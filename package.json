{
  "name": "vizjs",
  "version": "2.0.1",
  "description": "A visual regression tool for use with testing libraries.",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "babel src -d lib",
    "build:watch": "babel -w src -d lib",
    "test": "npm run build && jasmine",
    "docs": "jsdoc -d docs src/json-transformer.js"
  },
  "author": "Richard Harrison <mindintransti@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-plugin-transform-class-properties": "^6.9.1",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "babel-preset-es2015": "^6.9.0",
    "jasmine": "^2.4.1",
    "jsdoc": "^3.4.0",
    "rimraf": "^2.5.2"
  },
  "dependencies": {
    "mkdirp": "^0.5.1"
  }
}
